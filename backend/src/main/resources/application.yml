# Spring Boot 应用配置
spring:
  application:
    name: leafpan-backend

  # 生产数据库配置
  datasource:
    url: jdbc:mysql://mysql-service:3306/leafpan?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: leafpan
    password: leafpan123
    driver-class-name: com.mysql.cj.jdbc.Driver

  # # 开发数据库配置
  # datasource:
  #   url: jdbc:mysql://localhost:3306/leafpan?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
  #   username: root
  #   password: 123456
  #   driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA配置
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        
  # Jackson配置
  jackson:
    default-property-inclusion: non_null
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 150MB
      max-request-size: 150MB
      
  # Redis配置
  redis:
    host: redis-service
    port: 6379
    password: redis123
    database: 0
    timeout: 3000
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0

---
# 测试环境配置
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
    
  # JPA配置
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        
  # Jackson配置
  jackson:
    default-property-inclusion: non_null
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 150MB
      max-request-size: 150MB
      
  # Redis配置
  redis:
    host: redis-service
    port: 6379
    password: redis123
    database: 0
    timeout: 3000
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
      
# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /api
    
# MinIO配置
minio:
  endpoint: http://minio-service:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: leafpan
  # 两个桶配置
  data-bucket: data
  avatar-bucket: avatar
  
# 邮件配置
email:
  sender: 3555844679@qq.com
  password: xtibpzrdwnppchhi
  smtp:
    server: smtp.qq.com
    port: 465
  
# 应用自定义配置
app:
  # JWT配置
  jwt:
    secret: leafpan-secret-key-for-jwt-token-generation
    expiration: 86400000  # 24小时
    
  # 文件存储配置
  storage:
    max-file-size: 150MB
    
# 日志配置
#logging:
#  level:
#    com.yangshengzhou.backend: DEBUG
#    org.springframework.security: DEBUG
#    org.springframework.web: DEBUG
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"